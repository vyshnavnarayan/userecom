import 'package:flutter/material.dart';

class LikedItemPage extends StatelessWidget {
  const LikedItemPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('data')),
    );
  }
}